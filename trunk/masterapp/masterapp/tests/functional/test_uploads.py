from masterapp.tests import *

class TestUploadsController(TestController):

    """There is no index on the uploads controller
    def test_index(self):
        response = self.app.get(url_for(controller='uploads'))
        # Test response...
    """
